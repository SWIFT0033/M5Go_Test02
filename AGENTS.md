# M5Go_Test02 Project Rules

このファイルのルールは、`M5Go_Test02` リポジトリ全体に適用する。

## Arduinoスケッチ

- メインスケッチは `M5Go_Test02.ino` とし、M5Stackライブラリを使用する。
- ユーザーが明示的に変更を求めていない既存機能は維持する。
- スケッチを変更したら、`README.md` の現在の動作仕様も一致するよう更新する。
- ユーザーから見える機能変更は、`README.md` の `History` セクションへ追記する。新しい履歴を上に置く。
- 不要なファイルを作成・変更しない。

## コンパイルと実機書き込み

- スケッチ変更後は、次のFQBNでArduino CLIコンパイルを実行する。
  - `esp32:esp32:m5stack-core-esp32`
- コンパイルが失敗している状態では実機へ書き込まない。
- 書き込み直前に `arduino-cli board list` で接続ポートを再確認する。
- 書き込み前に、対象ボード、FQBN、ポート、完全なuploadコマンドをユーザーへ表示し、明示確認を得る。
- ポートを推測だけで決定しない。識別できない場合はユーザーへ確認する。
- 書き込み後は、CLIの終了コード、ハッシュ検証、ポート再接続を確認する。
- LCD表示、ボタン、音声などの物理的な動作は、CLI成功と区別してユーザーへ実機確認を依頼する。

## GitとGitHub

- 作業開始時に `git status` を確認し、既存の未コミット変更を保護する。
- コミット前に `git diff --check`、変更差分、変更ファイル一覧を確認する。
- 要求されたファイルだけを明示的に `git add` する。
- ユーザー指定のコミットメッセージがある場合は、その内容を使用する。指定がない場合は変更内容を表す短いメッセージを付ける。
- Push前に現在のブランチと `git remote -v` を確認する。
- このプロジェクトのPush先は次のリポジトリとする。
  - `https://github.com/SWIFT0033/M5Go_Test02.git`
  - ブランチ: `origin/main`
- GitHubへのPushは、現在の依頼でユーザーがPushまたはGitHub更新を求めた場合にのみ実行する。
- Push後は、ローカル `main` と `origin/main` のコミットIDが一致することを確認する。

## 完了報告

- 変更ファイル、追加・変更した機能、コンパイル結果、コミットID、Push結果、同期状態を簡潔に報告する。
- 実機書き込みを行った場合は、対象ポートと書き込み結果も報告する。
